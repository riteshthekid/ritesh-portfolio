# Backend Dependencies (Node.js/npm)
express
prisma
@prisma/client
pg
cors
helmet
jsonwebtoken
bcryptjs
zod
dotenv
express-rate-limit
express-async-errors
morgan

# Frontend Dependencies (Node.js/npm)
react
react-dom
react-router-dom
tailwindcss
vite
@vitejs/plugin-react
lucide-react
motion
@google/genai

# Note:
# This project is built using JavaScript/TypeScript (Node.js ecosystem).
# Dependencies are traditionally managed and installed via `package.json` 
# using the commands `npm install` inside the /frontend and /backend directories.
# A `requirements.txt` file is typically used for Python projects (via pip).
