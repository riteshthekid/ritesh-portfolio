{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "prisma": "^5.7.1",
    "@prisma/client": "^5.7.1"
  }
}
